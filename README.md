## Запуск проекта в DEV режиме

1. Устакновка зависимостей
   `npm install`

2. Запуск проекта
   `npm run start`

## Сборка проекта для production

1. Устакновка зависимостей
   `npm install`

2. Сборка проекта
   `npm run build`
